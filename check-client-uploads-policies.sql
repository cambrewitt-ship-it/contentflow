-- Check existing RLS policies on client_uploads table
SELECT 
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd,
    qual,
    with_check
FROM pg_policies 
WHERE tablename = 'client_uploads'
ORDER BY policyname;
